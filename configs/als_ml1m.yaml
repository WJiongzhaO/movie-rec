data:
  source: movielens
  params: {}
split:
  test_ratio: 0.2
model:
  name: als
  params:
    n_factors: 50
    reg: 0.1
    n_epochs: 15
    seed: 42
eval:
  k: 10
  relevance_threshold: 4.0
note: "真实 MovieLens 1M - ALS 交替最小二乘 (首次调优待进行)"
