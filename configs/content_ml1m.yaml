data:
  source: movielens
  params: {}
split:
  test_ratio: 0.2
model:
  name: content
  params:
    like_threshold: 4.0
eval:
  k: 10
  relevance_threshold: 4.0
note: "真实 MovieLens 1M - 基于内容(genres TF-IDF)"
